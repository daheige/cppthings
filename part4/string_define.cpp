#include <iostream>
#include <string>
using namespace std;

int main() {
    string s = "abc";  // 直接定义string
    cout << "s is : " << s << endl;
    // 字符串长度直接调用length方法获取
    cout << "s len = " << s.length() << endl;
    return 0;
}
